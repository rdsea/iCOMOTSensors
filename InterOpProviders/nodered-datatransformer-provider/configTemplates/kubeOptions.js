//some specific options for dealing with
//different kubernetes deployment
var kubeoptions = {
    "minikube_nodeport": true,
    //max number of instances, can be configured
    "max_instances": 3
}
export default kubeoptions;
